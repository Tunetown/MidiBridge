services:
  midibridge-python-test:
    build: .
    container_name: python3
    command: sleep infinity
    #ports:
      #- "80:80"
    volumes:
      - ../lib:/project/lib
      - ./bin:/project/bin
      - .:/project/test
